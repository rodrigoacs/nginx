server {
	listen 80;
	server_name api.caalb.dev www.api.caalb.dev;

	location /.well-known/acme-challenge/ {
		root /var/www/certbot;
	}

	location / {
		return 301 https://$host$request_uri;
	}
}

server {
	listen 443 ssl;
	http2 on;
	server_name api.caalb.dev www.api.caalb.dev;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;
	
	ssl_certificate /etc/letsencrypt/live/api.caalb.dev/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/api.caalb.dev/privkey.pem;
	include /etc/letsencrypt/options-ssl-nginx.conf;
	ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

	location /.well-known/acme-challenge/ {
		alias /var/www/certbot/;
	}

	location / {
		set $service_url http://newsletter-back-app-1:8001;
		proxy_pass $service_url;
	}
}
